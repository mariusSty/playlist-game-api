services:
  db-test:
    image: postgres:16-alpine
    ports:
      - '5434:5432'
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: playlist_game_test
    tmpfs:
      - /var/lib/postgresql/data
